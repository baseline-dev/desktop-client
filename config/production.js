const config = {
  service: {
    allowOrigin: ['https://baseline.dev', 'https://www.baseline.dev']
  },
  baselineUrl: 'https://baseline.dev/baseline',
  baselineStaticAssetsUrl: 'https://static.baseline.dev/latest',
  baselineApiUrl: 'https://api.baseline.dev'
};

export {
  config as default
}