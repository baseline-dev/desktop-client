import test from 'tape';

import {runPublicPrivateKeyFlow} from '../../src/util/keys';

test('Placeholder', t => {
  t.plan(1);
  t.equal(typeof runPublicPrivateKeyFlow, 'function', 'Baseline is a function');
});
